#include <bits/stdc++.h>
using namespace std;
#include <atcoder/all>
using namespace atcoder;
#define rep(i,n) for (int i=0;i<(int)(n);i++)
using ll=long long;
using P=pair<int,int>;

int main(){
    int n,k;
    cin >>n >>k;
    vector <int> p(n);
    for(int i=0;i<n;i++){
        cin >> p.at(i);
    }
    int ans;

    for(int i=0;i<n;i++){
        for(int j=i+1;j<n;j++){
            if(i<)
        }
    }

    cout << ans <<endl;
    return 0;
}
